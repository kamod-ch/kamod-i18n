{
  "name": "@kamod-ch/i18n-docs",
  "private": true,
  "description": "PreactPress documentation and live example for @kamod-ch/i18n.",
  "type": "module",
  "scripts": {
    "brand:sync": "kamod-brand-sync-docs --target public",
    "predev": "pnpm run brand:sync",
    "prebuild": "pnpm run brand:sync",
    "dev": "preactpress dev",
    "check": "preactpress check",
    "build": "preactpress build",
    "preview": "preactpress preview",
    "typecheck": "tsc --noEmit",
    "test": "preactpress check"
  },
  "dependencies": {
    "@kamod-ch/brand": "^0.1.9",
    "@kamod-ch/i18n": "workspace:*",
    "preact": "^10.25.3"
  },
  "devDependencies": {
    "@kamod-ch/preactpress": "^2.2.1",
    "typescript": "^5.7.2"
  }
}
